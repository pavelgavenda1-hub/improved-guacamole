name: geostone
description: GeoStone MVP mobile app
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.2.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.3
  google_maps_flutter: ^2.5.0
  qr_code_scanner: ^1.0.1
  image_picker: ^1.0.7
  geolocator: ^10.1.0
  flutter_dotenv: ^5.1.0
  flutter_hooks: ^0.20.5
  intl: ^0.19.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/
    - .env
